import socket
#pip install socket
s=socket.socket(socket.AF_INET , socket.SOCK_STREAM)
target = input("write url to scan:")
def namedef(parametr):
    commands
def pscan(port):
    try:
       taha=s.connect((port,target))
return True
except:
    return False
boolean = True-false
for x in range(1,50):
    if pscan(x)
       print("port => ",x,"is open")
else:
print("port => , x,"is close")
